<a
    href="{{ link(on) ~ (at ? ('#' ~ at)) }}"
    class="ms-2 text-decoration-none text-body-secondary"
    aria-label="Permalink"
    title="Permalink"
>
    <i class="fas fa-link small"></i>
</a>